apiVersion: v1
entries:
  redis:
    - name: redis
      description: Redis service
      version: 0.0.3
  # lambda-rc:
  #   - name: lambda-rc
  #     description: Lambda redis client
  #     version: 0.0.1